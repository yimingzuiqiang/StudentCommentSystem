# 1. VS IDE的本地配置/缓存文件夹（必须忽略）
.vs/

# 2. 编译输出目录（你的项目里是x64，包含Debug/Release产物）
x64/

# 3. VS项目的本地用户配置（每个开发者的本地调试配置，忽略）
*.vcxproj.user

# 4. C++编译产物（中间文件+可执行文件）
*.obj       # 目标文件
*.exe       # 可执行文件
*.pdb       # 调试符号文件
*.ilk       # 增量链接文件
*.lib       # 静态库
*.dll       # 动态库
*.log       # 编译日志
*.tmp       # 临时文件


# 5. 其他可能的临时文件
*.suo       # VS旧版本的解决方案用户选项（新版在.vs里，可加可不加）